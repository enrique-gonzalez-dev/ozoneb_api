Kaminari.configure do |config|
  # Default items per page for pagination
  config.default_per_page = 25
  # Optional: you can set other pagination defaults here
  # config.max_per_page = 100
  # config.window = 4
end
